# chatbot.conf — external paths and environment settings

# ChromaDB location (where ingested documents are stored)
CHROMA_PERSIST_DIR="/home/zappalaj/Bot_Test/ingestion_rag/SPEAR-RAG-Ingestion/chroma_db"

# ChromaDB collection name
CHROMA_COLLECTION="nougat_merged"

# Sentence transformer model for embeddings
EMBED_MODEL="sentence-transformers/all-MiniLM-L6-v2"

# Conda environment name for the RAG service
CONDA_ENV="rag"

# Log directory for service logs (rag.log, mcp.log)
LOG_DIR="/home/zappalaj/Chatbot_03122026/logs"
